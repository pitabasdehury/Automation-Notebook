package Java.AccessModifier.Default.Package2;

import Java.AccessModifier.Default.Package1.Student;

public class Test {
    public static void main(String[] args) {
     //   Student.number;     // Student class can be imported but can't call it's variable and method
    }
}
